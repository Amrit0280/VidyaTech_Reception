export const USER_PUBLIC_FIELDS = `
  u.id,
  u.school_id,
  u.name,
  u.email,
  u.phone,
  u.login_id,
  u.role,
  u.is_active,
  u.created_at
`;

export const ROLES = ["admin", "teacher", "student", "parent", "finance"];
